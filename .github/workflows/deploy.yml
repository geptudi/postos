name: Deploy Flutter Web (WASM)

on:
  workflow_dispatch:

permissions:
  contents: write
  actions: write

jobs:
  deploy:
    name: Build & Deploy
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Setup Flutter (exact version)
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.38.3'   # <--- SUA VERSÃO LOCAL
          cache: true

      - name: Install dependencies
        run: flutter pub get

      - name: Build Web (WASM)
        run: flutter build web --wasm --release --base-href=/postos/

      - name: Deploy to GitHub Pages
        id: deploy
        uses: JamesIves/github-pages-deploy-action@v4
        with:
          branch: gh-pages
          folder: build/web
          clean: true

<<<<<<< HEAD
      - name: Wait for GitHub Pages update
        run: sleep 10

      - name: Test version.json
        id: test_version
=======
      - name: Esperar atualização do GitHub Pages
        run: sleep 10

      - name: Testar site
        id: test
>>>>>>> 15e2ea2de9940cd88d7d53f3775f59b486699f0d
        run: |
          STATUS=$(curl -o /dev/null -s -w "%{http_code}" https://geptudi.github.io/postos/version.json)
          if [ "$STATUS" != "200" ]; then
<<<<<<< HEAD
            echo "version.json FAILED ($STATUS)"
            exit 1
          fi

      - name: Test WASM
        id: test_wasm
        run: |
          STATUS=$(curl -o /dev/null -s -w "%{http_code}" https://geptudi.github.io/postos/main.dart.wasm)
          if [ "$STATUS" != "200" ]; then
            echo "main.dart.wasm FAILED ($STATUS)"
            exit 1
          fi

      - name: Done
        run: echo "Deploy OK!"

  rollback_automatic:
=======
            echo "Erro: version.json não carregou"
            exit 1
          fi

          echo "Testando WASM..."
          WASM=$(curl -o /dev/null -s -w "%{http_code}" https://geptudi.github.io/postos/main.dart.wasm)
          if [ "$WASM" != "200" ]; then
            echo "Erro: main.dart.wasm não carregou"
            exit 1
          fi

          echo "Site OK!"

  rollback_automatico:
>>>>>>> 15e2ea2de9940cd88d7d53f3775f59b486699f0d
    name: Auto Rollback
    needs: deploy
    if: failure()
    uses: ./.github/workflows/rollback.yml
    secrets: inherit
